platform: linux

inputs:
  - name: source-code

run:
  path: sh
  args:
  - -exc
  - |
    whoami
    ls -lrt
    chmod -R 777 source-code
    cd source-code
    ls -lrt
    env
    pwd
    whoami
    mvnw -DskipTests=true clean package